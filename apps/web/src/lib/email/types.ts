// Transitional shim — types moved to @aira/email. Phase 5 (apps/web rewire)
// will rewrite callers to import from @aira/email/types directly.
export type { EmailDriver, SendArgs, SendResult } from "@aira/email/types"
