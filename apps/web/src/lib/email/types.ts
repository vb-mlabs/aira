// Transitional shim — types moved to @mlabs/email. Phase 5 (apps/web rewire)
// will rewrite callers to import from @mlabs/email/types directly.
export type { EmailDriver, SendArgs, SendResult } from "@mlabs/email/types"
