# Run log — landing-hero-3tier

- **2026-09-22** — Pre-flight complete. Branch `feat/landing-hero-v2` created from `main`. Docs commit `007b9a5` shipped the plan/review/mockups/artifacts. Tree clean. Starting Task 1.
- **2026-09-22 T1** — `NEXT_PUBLIC_LANDING_HERO_V2` added to `env.ts` `client:` + `runtimeEnv`; entry documented in root `.env.example`. `pnpm --filter @aira/web typecheck` green; lefthook (check-migrations + check-contrast) green. Commit `9f1e066`.
- **2026-09-22 T2** — `LaunchOfferDialog` extracted from `business-cta-pair.tsx` into its own file with the `GetListedDialog` signature (`{ children, triggerClassName, triggerAriaLabel }`). Removed the moved lucide imports (`Star`, `Users`, `User`, `TrendingUp`, `ShieldCheck`) from the source file — no longer needed there. `BusinessCtaPair` trigger className preserved verbatim. typecheck green, lint reports only the 17 pre-existing unrelated warnings (0 new). Commit `4ee5cbb`.
- **2026-09-22 T3** — `hero-v2.tsx` written (292 lines). Server Component. `brand.tagline` drives the eyebrow, `brand.name` interpolated in every AIRA-referencing string. Owner CTAs reuse `GetListedDialog` + `LaunchOfferDialog` (one-click UX). CSS-only QR placeholders + `href="#"` store badges ship as decoration until real assets land. Ornamental leaves rendered desktop-only via `hidden md:block`. Mobile stacking order: phone first, then user column, then owner column. typecheck green, no new lint warnings. Commit `2212616`.
- **2026-09-22 T4** — `page.tsx` picks `HeroV2` vs `Hero` and app-live vs pre-launch title/description based on `env.NEXT_PUBLIC_LANDING_HERO_V2 === "1"`. `pnpm build` green; `/` still generates as static content (build output confirms `○ /`). Commit `c30d3bd`.
- **2026-09-22 T5** — `apps/web/.env.local` created with `NEXT_PUBLIC_LANDING_HERO_V2=1` so this workspace's `pnpm dev` shows HeroV2 by default. `git check-ignore` confirms the file matches `.env*` on `.gitignore` line 34 — no commit produced. Side effect logged here; no ledger commit for this task.
- **2026-09-22 wrap** — All 5 tasks green. 4 code commits on top of the docs commit. Writing report.md, flipping plan status → `implemented`, then a final `docs(mstack)` commit for the ledger + report.
