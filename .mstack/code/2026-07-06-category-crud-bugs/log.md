# Run log — Category CRUD bugs (group B)

- 2026-07-06 pre-task: committed plan + review + learnings as `9d8c8cc chore(mstack): plan + review for category CRUD bugs (group B)`. Continuing on branch `feat/featured-business-selection` alongside group A per user's request.
- Task 1 (`6f1bcaf`): registered `business.category_slug_cascaded` audit kind at all four required sites (AuditMeta union, KNOWN_AUDIT_ACTIONS array, AUDIT_ACTION_LABEL_OVERRIDES map, render-detail.tsx switch case). Ships first so Task 2 can emit the kind without breaking the exhaustiveness assertion.
- Task 2 (`7af2ae6`): added `updateCategoryWithCascade` in `packages/services/src/categories/queries.ts`. Row-lock via `.for("update")` (drizzle-orm@0.45.2 supports it, verified in select.d.ts). Same transaction updates category + affected businesses + writes N audit rows. Deleted `assertSlugRenameAllowed` + its test file; replaced with `categories-rename-cascade.test.ts` covering 6 cases. Initial test run failed 3 cases because the mock mutated `state.categories[0]` in-place during the first `.set()` call, so the subsequent businesses filter saw the new slug — fixed by returning a merged row without mutating state (matches how RETURNING works pre-commit in real Postgres).
- Task 3 skipped: user picked "affordance only" over seeding a starter set. No SQL migration written.
- Task 4 (`0b86965`): added `assertCategoryIsSubcategory(db, slug)` in businesses/queries.ts. Three error codes surface: `category_required` (empty), `category_not_found` (unknown), `category_must_be_subcategory` (level=1 hit). Called from `createBusiness` (queries.ts) and `updateBusiness` (service.ts, only when `data.category !== undefined`).
- Task 5 (`2d4fe79`): swapped `apps/web/src/app/admin/businesses/new/page.tsx` from `listCategoriesOp` → `listCategoriesTreeOp`; `BusinessCreateForm` now takes `categoryTree` prop, renders primary picker as `<optgroup>` clusters of subs, drops standalone root options, and shows an affordance link either as empty-state or footnote. Same treatment on `CategoryEditModal` in business-detail.tsx — plus a disabled "current" option at top when the stored slug is a level-1 orphan (drift case) so admins don't lose sight of what they're moving away from. Roots with 0 active children render nothing (Safari empty-optgroup guard).
- Task 6 (`3cefaf4`): `apps/web/src/app/admin/settings/categories/new/page.tsx` reads `searchParams.parent`, validates it against fetched roots, passes as `defaultParentId` to `CategoryForm`. Form initializes `parentId` state from the prop when not in edit mode. Invalid ids silently drop.
- Polish (`928f402`): wired the modal affordance link to `/admin/settings/categories/new?parent=<rootId>` so Task 6's receiver actually fires from the natural entry point. Label changes to "Add a new subcategory under <Root> →" when a sub is selected. Closes the Task 5 ↔ Task 6 loop.

**Verification summary:** `pnpm --filter @aira/services typecheck && test`, `pnpm --filter @aira/web typecheck && test && lint` all clean at end of run. 6 tests in the new cascade test file pass. Lefthook migrations + contrast gates passed on every commit. No `--no-verify` bypasses used.
