# Run log — Featured business selection

- 2026-07-06 pre-task: created branch `feat/featured-business-selection` and committed plan + review + learnings + settings.json plugin artifacts as `40ec16e chore(mstack): plan + review for featured business selection`.
- Task 1 (`fc080bb`): added `getFeaturedRandom` + `getFeaturedRandomForCategory` with EXISTS-style predicates and `ORDER BY random() LIMIT n`. Deleted `getFeaturedBusinesses` and the `homepageSponsored{Flag,Priority,AmountCents}` SQL helpers (only that function referenced them). Skipped test-writing — the businesses/queries.ts module has no existing unit-test coverage across any of its functions, so writing tests just for the new ones would break pattern. Randomness / predicate verification deferred to `/mlabs-qa`. Also reverted a one-line comment fix in `packages/db/src/schema/businesses.ts` because the pre-commit migration hook rejects any change to schema files without an accompanying migration.
- Task 2 (`a953143`): rewired `listBusinessesOp` featured branch (both `featured=true` and the no-filter fallback) to route to the new queries. Added `FEATURED_LIMIT = 5` module constant to enforce the clamp uniformly. Rewrote the stale `BusinessListInputSchema.featured` JSDoc — it previously described the tier1/tier2-only behavior that hadn't been true for a while.
- Task 3 (`c3b9a6f`): home page limit 6→5 + `href="/directory"` → `href="/categories"`. Refreshed the page-level docstring.
- Task 4 (`55d7128`): mobile `FEATURED_LIMIT` 6→5 + updated `useFeatured` docstring.
- Task 5 (`a866e14`): new `PrimaryCategoryView` RSC component. Level branch on `/listings/[category]/page.tsx` — level 1 fetches through `listCategoriesRootsOp` (existing op, reused per review Concern #1) + featured op, renders `PrimaryCategoryView`. Level 2 unchanged.
- Task 6 (`a9b9847`): mobile mirror — new `PrimaryCategoryView` React Native component + `useFeaturedForCategory(slug)` hook + level branch on `listings/[category].tsx`. Mobile ListingView-style branch stayed intact; `useListings` naturally disables when `category=undefined`.

**Verification summary:** `pnpm --filter @aira/services typecheck && test`, `pnpm --filter @aira/web typecheck && lint`, `pnpm --filter @aira/mobile typecheck && lint`, `pnpm --filter @aira/api test` — all clean at end of run. Lefthook migrations + contrast gates passed on every commit. No `--no-verify` bypasses used.
